import type { OAuthProvider } from "@remcostoeten/auth-drawer";

/** OAuth providers enabled for both Better Auth server and the drawer adapter. Use [] for email/password only. */
export const oauthProviders = [] as OAuthProvider[];
